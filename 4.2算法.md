## [1. 两数之和](https://leetcode.cn/problems/two-sum/description/)

![image-20260401190856895](https://aaswk.oss-cn-beijing.aliyuncs.com/image-20260401190856895.png)

复习哈希表`unordered_map`的使用：

+ 什么时候用`unordered_map`：想查询一个元素是否出现过，本题我们既想知道该元素是否出现过又想知道它对应的下标，所以需要map中`key—value`的形式（不要求顺序，不涉及去重）
+ 怎样确定key和value：直接访问到的元素：本题就是直接访问数组的元素，用value存储它的下标（因为我们需要直接访问元素，判断目标值是否出现过，如果出现过，返回下标），有点像key是显形，value是隐形
+ 插入：`umap[key]=value`或`umap.insert({key,value})`或`umap.insert(pair<int,int>(key,value))`
+ 查找：`umap.find(key)!=umap.end()`
+ 删除：`umap.erase(key)`或`umap.erase(it)`
+ 遍历：`for(auto it=umap.begin();it!=umap.end();it++)`  `key用it->first表示`   `value用it->second表示`

```c++
class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        unordered_map<int,int> umap;
        for(int i=0;i<nums.size();i++){
            if(umap.find(target-nums[i])!=umap.end()){//查找目标元素
                return {i,umap[target-nums[i]]};//找到了，返回索引
            }
            umap.insert(pair<int,int>(nums[i],i));//没找到，把当前元素和索引加到哈希表中
        }
        return {};
    }
};
```

## [134. 加油站](https://leetcode.cn/problems/gas-station/description/)

![image-20260402112806099](https://aaswk.oss-cn-beijing.aliyuncs.com/image-20260402112806099.png)

暴力解法

枚举每个加油站编号为起始，绕一圈判断

```c++
class Solution {
public:
    int canCompleteCircuit(vector<int>& gas, vector<int>& cost) {
        int n=cost.size();
        for(int i=0;i<n;i++){//枚举出发时加油站的编号
            int sum=0;
            int j=i;
            int tmp=n;
            while(tmp){//保证每次都行驶一周（n次）
                sum+=gas[j%n]-cost[j%n];
                if(sum<0) break;//油不够了，枚举下一个编号
                j++;
                tmp--;
            }
            if(sum>=0) return i;//行驶完一周后，油依旧为够用，输出此次出发编号
        }
        return -1;//没找到编号，无法行驶一周
    }
};
```

贪心算法

从第一个加油站累加剩余油量，如果油不够用，一定是从下一个加油站起始才有可能行驶完一周

```c++
class Solution {
public:
    int canCompleteCircuit(vector<int>& gas, vector<int>& cost) {
        int curSum=0;//从起始加油站start到现在的剩余油量
        int sum=0;//总剩余量
        int start=0;//起始加油站索引
        
        for(int i=0;i<cost.size();i++){
            curSum+=gas[i]-cost[i];
            sum+=gas[i]-cost[i];//累加总剩余量
            if(curSum<0){
                start=i+1;//下一个加油站为起始加油站
                curSum=0;//剩余油量清零
            }
        }
        if(sum<0) return -1;//总剩余量不够，无法行驶完一周
        return start;
    }
};
```

