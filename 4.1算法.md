## [376. 摆动序列](https://leetcode.cn/problems/wiggle-subsequence/description/)

![](https://aaswk.oss-cn-beijing.aliyuncs.com/image-20260329182806185.png)

+ 贪心思想：

  局部最优：“删除”单调坡度上的节点，这个坡度就有两个峰值

  整体最优：整个序列有最多的局部峰值，从而得到最长摆动序列

+ 怎样表示局部最优呢？

  通过相邻元素的差值表示坡度：正数为上坡，负数为下坡，相邻上（下）坡为单调坡度

  用`prediff`和`curdiff`维护摆动的坡度（`prediff`为上一个摆动坡度，`curdiff`为当前坡度）只要出现一方大于0，一方小于0的情况，结果变量`res`加1

+ 细节注意：

1. 上下坡/单调坡中有平坡：如1-2-2-2-1，我们是删除前两个2，还是后两个2，这里我们删除前两个2，所以条件为`(prediff>=0&&curdiff<0)||(prediff<=0&&curdiff>0)`
2. 数组首尾两端：如果只有1个元素，就特判序列长为1；首：我们将初始`prediff`值设为0，假设序列前有一个平坡，这样只要第二个数和第一个数不相等，结果就会加1；尾：将结果变量`res`的初始值设为1

```c++
class Solution {
public:
    int wiggleMaxLength(vector<int>& nums) {
        int n=nums.size();
        if(n==1) return 1;//特判
        int prediff=0;
        int curdiff;
        int res=1;
        for(int i=1;i<n;i++){
            curdiff=nums[i]-nums[i-1];
            if((prediff>=0&&curdiff<0)||(prediff<=0&&curdiff>0)) {
                res++;
                prediff=curdiff;//保证当前坡度一定和上一个摆动坡度进行比较
            }
        }
        return res;
    }
};
```

## [3. 无重复字符的最长子串](https://leetcode.cn/problems/longest-substring-without-repeating-characters/description/)

![image-20260401183229562](https://aaswk.oss-cn-beijing.aliyuncs.com/image-20260401183229562.png)

```c
int max(int x,int y){
    if(x>y) return x;
    return y;
}
int lengthOfLongestSubstring(char* s) {
    int cnt[256]={0};//哈希数组：计算字符出现次数
    int left=0;//滑窗左端
    int ans=0;
    for(int i=0;s[i]!='\0';i++){//枚举滑窗右端
        cnt[s[i]]++;
        while(cnt[s[i]]>1){//如果新加入的元素出现过，移动滑窗左端，直至无重复字符
            cnt[s[left]]--;
            left++;
        }
        ans=max(ans,i-left+1);//计算滑窗长度
    }
    return ans;
}
```



```c++
class Solution {
public:
    int lengthOfLongestSubstring(string s) {
        unordered_map<char,int> cnt;//无序哈希表
        int left=0;
        int ans=0;
        for(int i=0;i<s.size();i++){
            cnt[s[i]]++;
            while(cnt[s[i]]>1){
                cnt[s[left]]--;
                left++;
            }
            ans=max(ans,i-left+1);
        }
        return ans;
    }
};
```





