## [257. 二叉树的所有路径 ](https://leetcode.cn/problems/binary-tree-paths/description/)

![image-20260315130547289](https://aaswk.oss-cn-beijing.aliyuncs.com/image-20260315130547289.png)

递归+回溯
整体思路：

1. 从根节点开始，沿着每一条分支向下遍历
2. 每访问一个节点，就把它的值加入当前路径的path
3. 如果当前节点是叶子节点，说明一条完整的路径已经形成，将path中的节点值按”val1->val2->...->vlan"的格式拼接成字符串，存入结果数组result
4. 如果当前节点不是叶子节点，则继续递归遍历它的左孩子和右孩子
    注意：递归返回后，需要将刚刚加入的节点值从path中弹出（即回溯）
  + 递归三部曲：
1. **明确递归函数的参数和返回值**，参数有当前传入的节点，路径path，结果集res
2. **明确终止条件**，当前节点为叶子节点
3. **明确单层递归逻辑**，将当前节点加入路径，左子树不为空就递归左子树，右子树不为空就递归右子树
  ```c++
class Solution {
public:
    void traversal(TreeNode*cur,vector<int>&path,vector<string>&res){
        //单层逻辑
        //先将当前节点加入路径
        path.push_back(cur->val);
        //递归左子树
        if(cur->left){
            traversal(cur->left,path,res);//递归
            path.pop_back();//回溯
        }
        //递归右子树
        if(cur->right){
            traversal(cur->right,path,res);
            path.pop_back();
        }
        //终止条件
        if(cur->left==nullptr&&cur->right==nullptr){
            string spath;
            for(int i=0;i<path.size()-1;i++){
                spath+=to_string(path[i]);
                spath+="->";
            }
            spath+=to_string(path[path.size()-1]);
            res.push_back(spath);
            return;
        }
    }
    vector<string> binaryTreePaths(TreeNode* root) {
        vector<string> res;
        vector<int> path;
        if(root==nullptr) return res;
        traversal(root,path,res);
        return res;
    }
};
  ```

## [77. 组合 ](https://leetcode.cn/problems/combinations/description/)

![image-20260315163446887](https://aaswk.oss-cn-beijing.aliyuncs.com/image-20260315163446887.png)

```c++
class Solution {
public:
    //如果不做全局变量定义，就需要作为递归函数的参数，并在所给函数内部在定义一次
    vector<int> path;
    vector<vector<int>> res;
    
    void backtracking(int startIndex,int n,int k){
        //终止条件
        if(path.size()==k){
            res.push_back(path);
            return;
        }
        //单层递归逻辑
        for(int i=startIndex;i<=n;i++){
            path.push_back(i);//处理节点，加入路径
            backtracking(i+1,n,k);//递归
            path.pop_back();//回溯
        }
    }
    
    vector<vector<int>> combine(int n, int k) {
        backtracking(1,n,k);//从1开始
        return res;
    }
};
```

