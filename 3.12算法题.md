## [200. 岛屿数量](https://leetcode.cn/problems/number-of-islands/description/)

![image-20260312182441249](https://aaswk.oss-cn-beijing.aliyuncs.com/image-20260312182441249.png)

网格DFS模版：

递归入口：岛屿第一行最左边的陆地

递归方向：左右上下的相邻陆地

递归边界：出界、遇到水、遇到之前走过的岛屿

```c++
void dfs(char**grid,int m,int n,int i,int j){
    if(i<0||i>=m||j<0||j>=n||grid[i][j]!='1') return ;//出界或者不是‘1’，就不再往下递归了
    grid[i][j]='2';//标记走过的岛屿为‘2’
    dfs(grid,m,n,i-1,j);//左走
    dfs(grid,m,n,i+1,j);//右走
    dfs(grid,m,n,i,j-1);//上走
    dfs(grid,m,n,i,j+1);//下走
}

int numIslands(char** grid, int gridSize, int* gridColSize) {
    int m=gridSize;//行数
    int n=gridColSize[0];//列数
    int ans=0;
    for(int i=0;i<m;i++){
        for(int j=0;j<n;j++){
            if(grid[i][j]=='1'){
                ans++;
                dfs(grid,m,n,i,j);
            }
        }
    }
    return ans;
}
```

## [572. 另一棵树的子树](https://leetcode.cn/problems/subtree-of-another-tree/description/)

![image-20260312183502438](https://aaswk.oss-cn-beijing.aliyuncs.com/image-20260312183502438.png)

对于二叉树root中的每个节点node，都与subRoot计算一次[100. 相同的树 ](https://leetcode.cn/problems/same-tree/description/)，如果有匹配的上的，则答案为true

```c++
class Solution {
public:
    //判断两棵树是否相同
    bool isSame(TreeNode*p,TreeNode*q){
        //终止条件
        if(p==nullptr&&q==nullptr) return true;
        else if(p==nullptr||q==nullptr) return false;
        else if(p->val!=q->val) return false;
        //左子树的左节点，右子树的右节点（完全相同，所以用“与”&&）
        return isSame(p->left,q->left)&&isSame(p->right,q->right);
    }
    bool isSubtree(TreeNode* root, TreeNode* subRoot) {
        if(root==nullptr) return false;
        if(isSame(root,subRoot)) return true;
        //在左子树或右子树找到相同的就返回，用“或”||（注意都是和subRoot比）
        return isSubtree(root->left,subRoot)||isSubtree(root->right,subRoot);
    }
};
```

